import { ArtefatonamerPipe } from './artefatonamer.pipe';

describe('ArtefatonamerPipe', () => {
  it('create an instance', () => {
    const pipe = new ArtefatonamerPipe();
    expect(pipe).toBeTruthy();
  });
});
