import { ArtefatoconferePipe } from './artefatoconfere.pipe';

describe('ArtefatoconferePipe', () => {
  it('create an instance', () => {
    const pipe = new ArtefatoconferePipe();
    expect(pipe).toBeTruthy();
  });
});
